// Define a Mongoose model for Country Formats
const mongoose = require('mongoose');
const FormatModelSchema = new mongoose.Schema({
    countryName: String,
    countryCode: String,
    format: String
});

module.exports = mongoose.model('FormatModel', SearchModelSchema);