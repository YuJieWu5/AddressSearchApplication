const mongoose = require('mongoose');

const SearchResultSchema = new mongoose.Schema({
    name: String, // Name of the person or entity
    address: {
        street: String,
        city: String,
        province: String,
        postalCode: String
    },
    country: String // Country code or name of the country
});

module.exports = mongoose.model('SearchResult', SearchResultSchema);